<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Modifier le projet</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/edit-projet.css">
</head>
<body>
  <div class="form-container">
    <h1>Modifier le projet 🛠️</h1>

    {{#if forbidden}}
      <div class="alert">Tu n'es pas autorisé à modifier ce projet.</div>
    {{else if notFound}}
      <div class="alert">Projet introuvable.</div>
    {{else}}
      <form method="POST" action="/pitch-deck/{{project.id}}/edit" enctype="multipart/form-data">
        <label for="file">Nom du projet :</label>
        <input type="text" id="file" name="file" value="{{project.file}}" required>

        <label for="amount">Montant :</label>
        <input type="number" id="amount" name="amount" value="{{project.amount}}" required>

        <label for="image">Changer l'image :</label>
        <input type="file" id="image" name="image" accept="image/*">

        <button type="submit">Enregistrer ✅</button>
      </form>
    {{/if}}

    <a class="back-link" href="/projets/{{project.id}}">⬅️ Retour au projet</a>
  </div>
</body>
</html>
