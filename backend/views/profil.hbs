<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mon profil</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/profil.css">
</head>
<body>

  <!-- ✅ Bouton retour accueil -->
  <a href="/accueil" class="back-button">← Retour à l’accueil</a>

  <div class="card">
    <h1>👤 Mon Profil</h1>
    <div class="info">
      <p><strong>Nom :</strong> {{user.name}}</p>
      <p><strong>Email :</strong> {{user.email}}</p>
      <p><strong>Rôle :</strong> {{user.role}}</p>
    </div>

    <form method="POST" action="/profil/password">
      <h2>🔐 Modifier mon mot de passe</h2>
      <input type="password" name="currentPassword" placeholder="Mot de passe actuel" required>
      <input type="password" name="newPassword" placeholder="Nouveau mot de passe" required>
      <button type="submit">Mettre à jour</button>
    </form>
  </div>

  <h2>📅 Mes rendez-vous</h2>

  {{#if meetings.length}}
    <ul>
      {{#each meetings}}
        <li>
          Rendez-vous pour le projet <strong>{{this.pitchName}}</strong>
          le {{this.date}}
        </li>
      {{/each}}
    </ul>
  {{else}}
    <p style="padding-left: 1rem;">Aucun rendez-vous prévu.</p>
  {{/if}}

</body>
</html>
