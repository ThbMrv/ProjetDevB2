<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Linkora® - Connecting Founders & Investors</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@700;400&display=swap" rel="stylesheet">
  <style>
    body, html {
      margin: 0;
      padding: 0;
      font-family: 'Inter', Arial, sans-serif;
      background: #151618;
      color: #e0e0e0;
      min-height: 100vh;
      overflow-x: hidden;
    }

    .landing-bg {
      position: fixed;
      top: 0; left: 0; right: 0; bottom: 0;
      z-index: 0;
      background: radial-gradient(ellipse 60% 40% at 60% 30%, #b0c3c8bb 0%, #101014 60%, #191921 100%);
      filter: blur(18px);
      pointer-events: none;
    }

    /* === HEADER MODERNE ET RESPONSIVE === */
    .main-nav {
      width: 100%;
      background: none;
      box-shadow: none;
      position: relative;
      left: 0;
      top: 0;
      z-index: 100;
      margin-bottom: 0;
      margin-bottom: 3%;
    }
    .main-nav-inner {
      max-width: 860px;
      margin: 22px auto 0 auto;
      background: transparent;
      border-radius: 20px;
      box-shadow: none;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 8px;
      min-height: 54px;
      position: relative;
      backdrop-filter: none;
      border: none;
      overflow-x: auto;
    }
    .nav-logo {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0;
      margin-right: 10px;
      border-radius: 99px;
      height: 38px;
      width: 38px;
      background: transparent;
      border: none;
    }
    .nav-logo svg {
      width: 26px;
      height: 26px;
    }
    .account-btn {
      background: linear-gradient(90deg, #232325 60%, #5e6264 120%);
      color: #fff;
      border: none;
      border-radius: 17px;
      padding: 8px 18px;
      font-size: 1.01rem;
      font-weight: 700;
      cursor: pointer;
      transition: background .2s, color .2s, box-shadow .19s;
      margin-left: 9px;
      min-width: 0;
      outline: none;
      border: 1px solid #39466150;
      box-shadow: 0 1px 4px #39466110;
    }
    .account-btn:hover, .account-btn:focus {
      background: linear-gradient(90deg, #44484a 0%, #232527 100%);
      color: #fff;
    }

    /* Adaptation mobile header */
    @media (max-width: 650px) {
      .main-nav-inner {
        max-width: 97vw;
        min-height: 40px;
        padding: 0 1vw;
        margin-top: 8px;
        border-radius: 13px;
        box-shadow: 0 1px 7px 0 #0008;
      }
      .nav-logo {
        width: 23px;
        height: 23px;
        min-width: 23px;
        margin-right: 3px;
      }
      .nav-logo svg {
        width: 18px;
        height: 18px;
      }
      .account-btn {
        font-size: 0.96rem;
        padding: 6px 10px;
        margin-left: 2px;
        border-radius: 11px;
        min-width: 0;
      }
    }

    /* HERO */
    .hero {
      min-height: 70vh;
      margin-top: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      position: relative;
      z-index: 2;
      padding-bottom: 20vw;
    }
    @media (max-width: 650px) {
      .hero {
        padding-top: 12vw;
        padding-bottom: 9vw;
      }
      .hero-content {
        max-width: 96vw;
        padding-left: 2vw;
        padding-right: 2vw;
      }
    }
    .hero-content {
      text-align: center;
      max-width: 620px;
      margin: 0 auto 24px auto;
      z-index: 4;
      position: relative;
      margin-top: 0;
    }
    h1 {
      font-size: 2rem;
      font-weight: 700;
      margin: 18px 0 8px 0;
      letter-spacing: -1.5px;
      line-height: 1.10;
      color: #fff;
    }
    .gradient-text {
      background: linear-gradient(90deg, #b0c3c8, #fff, #a0b1bc 80%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .fade-text {
      color: #b0b1b8cc;
      font-weight: 700;
    }
    .subtitle {
      color: #e6eaf6;
      font-size: 1.07rem;
      margin-bottom: 18px;
      margin-top: 0;
      font-weight: 400;
    }

    .cta-buttons {
      display: flex;
      flex-direction: column;
      gap: 10px;
      align-items: center;
      justify-content: center;
      margin-top: 14px;
    }
    .cta-dark, .cta-light {
      padding: 12px 28px;
      border-radius: 18px;
      font-size: 1.07rem;
      font-weight: 600;
      border: none;
      cursor: pointer;
      box-shadow: 0 4px 18px #10101433;
      transition: background 0.18s, color 0.18s;
      width: 100%;
      max-width: 320px;
      min-width: 120px;
      margin: 0 auto;
      text-align: center;
    }
    @media (max-width: 650px) {
      .cta-dark, .cta-light {
        font-size: 1rem;
        padding: 10px 8px;
        border-radius: 13px;
        max-width: 95vw;
        min-width: 95px;
      }
    }
    .cta-dark {
      background: #23242a;
      color: #fff;
    }
    .cta-dark:hover {
      background: #44474e;
    }
    .cta-light {
      background: #fff;
      color: #191921;
    }
    .cta-light:hover {
      background: #e2e3e5;
      color: #101014;
    }

    .mini-btn {
      background: #23242a;
      color: #fff;
      border-radius: 12px;
      padding: 7px 18px;
      font-size: .92rem;
      margin-bottom: 10px;
      border: none;
      font-weight: 600;
      box-shadow: 0 2px 10px #2224;
      cursor: pointer;
      letter-spacing: .5px;
      transition: background 0.15s;
    }
    .mini-btn:hover {
      background: #383a41;
    }

    .hero-stats {
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 18px;
      margin: 36px 0 12px 0;
      color: #e2e6f6;
      font-size: 1.15rem;
      opacity: 0.97;
      background: rgba(16,16,20,0.5);
      border-radius: 13px;
      box-shadow: 0 2px 18px #0003;
      padding: 14px 0 10px 0;
    }
    .hero-stats div {
      text-align: center;
    }
    .hero-stats strong {
      display: block;
      font-size: 2rem;
      font-weight: 700;
      letter-spacing: -1px;
      color: #e6f1ff;
      margin-bottom: 2px;
    }
    .hero-stats span {
      display: block;
      font-size: .93rem;
      color: #d0e0ee;
      font-weight: 500;
    }
    .hero-partners {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 16px;
      margin-bottom: 44px;
      opacity: .97;
      font-size: 1.09rem;
      flex-wrap: wrap;
      width: 100%;
      text-align: center;
    }
    .hero-partners span {
      color: #c9c9c9;
      font-weight: 500;
      margin-right: 5px;
      font-size: 1.09rem;
      flex-shrink: 0;
    }
    .hero-partners img {
      display: inline-block;
      vertical-align: middle;
      height: 22px;
      max-width: 70px;
      padding: 2px 2vw;
      margin: 0 2vw;
      background: #fff;
      border-radius: 7px;
      opacity: 0.93;
      box-shadow: 0 1px 6px #0003;
      object-fit: contain;
      filter: grayscale(0.2) contrast(1.05);
    }
    @media (min-width: 601px) {
      .hero-partners img {
        height: 28px;
        max-width: 120px;
        margin: 0 8px;
      }
    }

    /* Nodes effet - CACHÉS SUR MOBILE */
    .node {
      position: absolute;
      background: rgba(30,32,38, 0.7);
      border: 1.5px solid #45474e77;
      border-radius: 16px;
      padding: 9px 16px;
      color: #d0e5e6;
      font-size: .96rem;
      font-weight: 500;
      box-shadow: 0 4px 18px #10101444;
      z-index: 1;
      opacity: 0.95;
      display: none;
    }
    .node span { display: block; color: #fff7; font-size: .92rem; }
    @media (min-width: 601px) {
      .node {
        display: block;
      }
      .hero-stats {
        flex-direction: row;
        gap: 34px;
        margin: 44px 0 14px 0;
      }
      .hero {
        padding-bottom: 0;
      }
    }
    .node-left        { left: 4%;  top: 30%; }
    .node-bottom-left { left: 6%;  bottom: 18%; }
    .node-top-right   { right: 6%; top: 22%; }
    .node-bottom-right{ right: 5%; bottom: 15%; }

    .footer-logos {
      width: 100%;
      padding: 20px 4px 12px 4px;
      display: flex;
      justify-content: center;
      gap: 10px;
      align-items: center;
      background: transparent;
      opacity: .86;
      z-index: 10;
      position: relative;
      font-size: .99rem;
      flex-wrap: wrap;
    }
    .footer-logos img {
      height: 24px;
      opacity: 0.74;
    }
    @media (min-width: 600px) {
      .hero-content h1 { font-size: 2.3rem; }
      .cta-buttons { flex-direction: row; gap: 20px; }
      .footer-logos { font-size: 1.1rem; gap: 24px; }
    }
    @media (min-width: 900px) {
      .hero-content h1 { font-size: 3rem; }
      .footer-logos { gap: 44px; }
    }

    /* ===== Fade-in Animation pour effet d'apparition progressive ===== */
    @keyframes fadein {
      from { opacity: 0; transform: translateY(30px);}
      to   { opacity: 1; transform: translateY(0);}
    }
    .fadein    { opacity: 0; animation: fadein 1.1s cubic-bezier(.29,0,.37,1) 0.08s forwards;}
    .fadein-1  { opacity: 0; animation: fadein 1.1s cubic-bezier(.29,0,.37,1) 0.23s forwards;}
    .fadein-2  { opacity: 0; animation: fadein 1.1s cubic-bezier(.29,0,.37,1) 0.39s forwards;}
    .fadein-3  { opacity: 0; animation: fadein 1.1s cubic-bezier(.29,0,.37,1) 0.53s forwards;}
    .fadein-4  { opacity: 0; animation: fadein 1.1s cubic-bezier(.29,0,.37,1) 0.68s forwards;}
    .fadein-5  { opacity: 0; animation: fadein 1.1s cubic-bezier(.29,0,.37,1) 0.85s forwards;}
    .fadein-6  { opacity: 0; animation: fadein 1.1s cubic-bezier(.29,0,.37,1) 1.0s forwards;}
  </style>
</head>
<body>
  <div class="landing-bg fadein"></div>
  <header class="fadein-1">
    <nav class="main-nav">
      <div class="main-nav-inner">
        <a href="#" class="nav-logo"></a>
        <a href="/login" class="account-btn">Create Account</a>
      </div>
    </nav>
  </header>
  <main>
    <section class="hero fadein-2">
      <div class="hero-content">
        <h1 class="fadein-3"><span class="gradient-text">Linkora®</span></h1>
        <button class="mini-btn fadein-3">Build your network of opportunities →</button>
        <h1 class="fadein-4">
          <span class="gradient-text">Connect</span><br>
          with investors and projects<br>
          <span class="fade-text">of tomorrow</span>
        </h1>
        <p class="subtitle fadein-5">
          The platform where <b>founders</b> meet their <b>investors</b>, and investors discover the next big opportunities.<br>
          Take action, create your financial future.
        </p>
        <div class="cta-buttons fadein-6">
          <button class="cta-dark">I'm looking for investors</button>
          <button class="cta-light">I want to invest</button>
        </div>

        <!-- STATS SECTION -->
        <div class="hero-stats fadein-4">
          <div>
            <strong>312</strong>
            <span>projects funded</span>
          </div>
          <div>
            <strong>124</strong>
            <span>active investors</span>
          </div>
          <div>
            <strong>€7.5M</strong>
            <span>raised</span>
          </div>
        </div>

        <!-- PARTNERS SECTION -->
        <div class="hero-partners fadein-5">
          <span>Trusted by:</span>
          <img src="/Image PNG.png" alt="Meta" />
          <img src="/Image PNG 3.png" alt="Microsoft" />
          <img src="/Image PNG 2.png" alt="Paypal" />
        </div>
      </div>
      <!-- Nodes (hidden on mobile, shown on desktop only) -->
      <div class="node node-left fadein-4">AI Project<br><span>Seeking €1.2M</span></div>
      <div class="node node-bottom-left fadein-4">Healthcare<br><span>€500K</span></div>
      <div class="node node-top-right fadein-4">GreenTech<br><span>€850K</span></div>
      <div class="node node-bottom-right fadein-4">EdTech<br><span>€300K</span></div>
    </section>
    <footer class="footer-logos fadein-6">
      <span>🚀 Secure & Fast</span>
      <span>🤝 Instant Networking</span>
      <span>📱 100% Mobile</span>
    </footer>
  </main>
</body>
</html>
